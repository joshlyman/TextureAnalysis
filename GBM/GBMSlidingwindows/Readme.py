# This package is to GBM sliding windows textures version 1,
#
# difference with version 2: boundary windows definition: if boundary window center coordinate is in boundary, then it is boundary window
